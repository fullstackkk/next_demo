
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Contacts",
  };

export default function Contacts() {
  return (
    <main className="flex justify-center items-start w-[100vw] px-10">
      Contacts
    </main>
  );
}
